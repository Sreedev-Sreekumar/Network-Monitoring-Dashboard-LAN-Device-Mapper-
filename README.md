# Network-Monitoring-Dashboard-LAN-Device-Mapper-
scans the local network to detect active devices and display network activity in real time. The system performs LAN discovery using ICMP ping requests and identifies connected devices by analyzing IP address patterns. The dashboard provides a clear overview of network connectivity and device status through an interactive interface.
